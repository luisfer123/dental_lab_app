package com.dentallab.security.service;

import org.springframework.stereotype.Service;

/**
 * Stub service for refresh token management.
 * If tokens are stored in DB, implement revoke/cleanup here.
 * For HttpOnly cookies only, this can remain empty.
 */
@Service
public class RefreshTokenService {
}
