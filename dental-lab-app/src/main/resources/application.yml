spring:
  profiles:
    active: dev   # default profile when you run locally
  jpa:
    hibernate:
      ddl-auto: none   # schema already exists in database.sql
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect